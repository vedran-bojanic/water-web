import { MultiplyElementPipe } from './multiply-element.pipe';

describe('MultiplyElementPipe', () => {
  it('create an instance', () => {
    const pipe = new MultiplyElementPipe();
    expect(pipe).toBeTruthy();
  });
});
